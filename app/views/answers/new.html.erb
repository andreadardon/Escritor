<%= form_for @answer do |a| %>

  Escribe tu respuesta aquí:
  <%= a.text_area(:body, style: "width: 500px; height: 300px;" ) %>
  <%= a.hidden_field(:exercise_id, value: @exercise.id) %>


  <%= a.submit 'Guardar' %>
<% end %>